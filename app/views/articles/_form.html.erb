<%= render 'errors' %>

<%= form_with(model: @article, local: true) do |f| %>
    <p>
        <%= f.label "Title" %> <br>
        <%= f.text_field :title %>
    <p>
    <p>
        <%= f.label "Desciption" %> <br>
        <%= f.text_area :description %>
    </p>
    <p>
        <%= f.submit %>
    </p>
<% end %>